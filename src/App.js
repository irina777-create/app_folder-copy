import React from "react";
const cars=[

];

const App=() =>{
  return (
    <div className="contaner">
      <h1>Car</h1>
      <div
          {cars.map((item.index)=>{
              return
              <React.Fragment key=
              {index}=>
          <Table
              brand={item.brand}
              model={item.model}
              />
      </React.Fragment>


    </div>
  );
}

export default App;

}
 